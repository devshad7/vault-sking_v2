// "use client";

// import { getProducts } from "@/lib/frontend-data";

// export function useVariantProducts(variant: string) {
//   const products = getProducts().filter((product) => product.variant === variant);

//   return {
//     products,
//     isLoading: false,
//     isError: false,
//   };
// }
